version: 2

jobs:
    build:
        docker:
            - image: nicolov/gazebo-headless-docker:0.0.1
        steps:
            - checkout
            - run:
                scripts/run-demo.sh
            - store_artifacts:
                path: out

